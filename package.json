{
  "name": "test-project",
  "version": "1.0.0",
  "description": "Test automation framework based on Protractor",
  "main": "config.js",
  "license": "ISC",
  "directories": {
    "test": "specs"
  },
  "dependencies": {},
  "devDependencies": {
    "allure-commandline": "^2.7.0",
    "jasmine-allure-reporter": "^1.0.2",
    "protractor": "^5.4.0"
  },
  "scripts": {
    "pretest": "rm -rf allure-report",
    "test": "protractor config.js",
    "posttest": "./node_modules/allure-commandline/bin/allure generate allure-results --clean -o allure-report || true"
  },
  "author": "Anna Shvets"
}
